# Nixpacks auto-detects Python + Poetry from pyproject.toml and handles
# the install phase. We only need to add the spaCy model download.

[phases.build]
cmds = ["python -m spacy download en_core_web_sm"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
